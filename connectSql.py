# import pymysql
# db = pymysql.connect("localhost","root","password","test" )
# cursor = db.cursor()
# cursor.execute("SELECT * FROM USER")
# data = cursor.fetchall()

# print (data)
# import json
# list =[]
# list.append(json.dumps({"a":'A',"b":'B','date':'2017-ss'}))
# print(list,'list')
# print(json.dumps({"a":'A',"b":'B'}))
